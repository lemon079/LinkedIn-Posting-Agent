---
phase: 02-react-dashboard-ui
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: ["client/src/App.tsx", "client/src/components/LinkedInFeed.tsx", "client/src/components/EditorPanel.tsx"]
autonomous: true
requirements: ["UI-02", "UI-03"]
must_haves:
  truths:
    - "GET /api/topics is called on load"
    - "POST /api/draft creates a draft displayed in the editor"
    - "POST /api/publish resumes the graph run and displays final link"
  artifacts:
    - path: "client/src/App.tsx"
      provides: "HTTP fetch integrations with Express server"
      contains: 'fetch\("/api'
  key_links: []
---

<objective>
Connect the React dashboard to the backend REST API, loading topic dropdown choices, triggering post drafting, showing drafts in a simulated high-fidelity LinkedIn post card, and supporting final live edits and publication.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-react-dashboard-ui/02-CONTEXT.md
@.planning/phases/02-react-dashboard-ui/02-DISCUSSION-LOG.md
@.planning/phases/02-react-dashboard-ui/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Load topics on frontend load</name>
  <files>client/src/App.tsx</files>
  <read_first>client/src/App.tsx</read_first>
  <action>Open client/src/App.tsx. Add a useEffect hook to trigger a GET fetch to `/api/topics` immediately upon component mount. Update the topics selection state array, and populate the dropdown menu dynamically with the returned genres.</action>
  <verify>Dropdown contains AI, Machine Learning, etc.</verify>
  <acceptance_criteria>
    - App component fetches /api/topics on mount and updates state
  </acceptance_criteria>
  <done>GET /api/topics integration fetches and populates the topic list dropdown</done>
</task>

<task type="auto">
  <name>Task 2: Build LinkedIn Feed card simulator component</name>
  <files>client/src/components/LinkedInFeed.tsx</files>
  <action>Create client/src/components/LinkedInFeed.tsx. Render a simulated high-fidelity LinkedIn feed card using Tailwind classes for layout, spacing, and styling: Profile header (member avatar circle, name, description, relative time with globe icon), post commentary block (`white-space: pre-wrap` to preserve formatting), and mock action buttons (Like, Comment, Repost, Send) with standard mockup SVG icons. Keep under 80 lines.</action>
  <verify>LinkedInFeed component renders correctly with Tailwind styling</verify>
  <acceptance_criteria>
    - Simulated card displays member details, post text, and action buttons in high-fidelity LinkedIn style
  </acceptance_criteria>
  <done>LinkedIn Feed card simulator component created</done>
</task>

<task type="auto">
  <name>Task 3: Build Live Editor panel component using shadcn UI</name>
  <files>client/src/components/EditorPanel.tsx</files>
  <action>Create client/src/components/EditorPanel.tsx. Import and use Shadcn's Textarea, Button, and Card components. The component accepts draftText, onChange, isPublishing, and onPublish. Inside a Card, render a Textarea for editing, a character count label (0/3000) that adds Tailwind styling warnings when exceeding 2800 (text-yellow-500) or 3000 (text-red-500) characters, and a Publish button. Keep under 80 lines.</action>
  <verify>EditorPanel renders and tracks length with warning states</verify>
  <acceptance_criteria>
    - Textarea updates character count dynamically
    - Publish button is disabled if characters exceed 3000
  </acceptance_criteria>
  <done>Live Editor panel component created using shadcn UI controls</done>
</task>

<task type="auto">
  <name>Task 4: Integrate drafting and publishing API endpoints</name>
  <files>client/src/App.tsx</files>
  <read_first>client/src/App.tsx</read_first>
  <action>Open client/src/App.tsx. Import `LinkedInFeed` and `EditorPanel` components. Implement `handleGenerate`: trigger a POST fetch to `/api/draft` with topic and context, setting loading spinner state while fetching, and capturing returned `threadId` and `draft` content. Implement `handlePublish`: trigger a POST fetch to `/api/publish` with `threadId` and final edited `draft` content, capturing and displaying the returned `postUrl`. Bind components and states into the right panel outcome block using Tailwind classes.</action>
  <verify>client/src/App.tsx successfully routes api calls and compiles</verify>
  <acceptance_criteria>
    - Clicking generate fetches draft and renders the preview and editor
    - Clicking publish saves changes and displays the post link
  </acceptance_criteria>
  <done>API fetch integrations and component binding completed</done>
</task>

</tasks>

<verification>
- [ ] TypeScript compilation compiles cleanly (npx tsc --noEmit in client/)
- [ ] Endpoints GET /api/topics, POST /api/draft, and POST /api/publish successfully called and handle state updates
</verification>

<success_criteria>
- Frontend React card simulated layout matching authentic LinkedIn style features
- Dual view displaying live editable copy and instant high-fidelity card preview
- Character limit tracking up to 3000 characters with warning styles
- Publishing on-demand with instant link display
</success_criteria>

<output>
Create `.planning/phases/02-react-dashboard-ui/02-02-SUMMARY.md` on completion.
</output>
