---
phase: quick
plan: "260622-ja9"
type: execute
wave: 1
depends_on: []
files_modified: ["client/src/components/SettingsPanel.tsx"]
autonomous: true
requirements: []
must_haves:
  truths:
    - "Vite React client compiles and starts"
  artifacts:
    - path: "client/src/components/SettingsPanel.tsx"
      provides: "Dynamic model fetching UI for Ollama"
      contains: "ollamaFetchState"
  key_links: []
---

<objective>
Implement dynamic model fetching for local Ollama instances in the settings panel with comprehensive edge-case handling.
1. Add an `ollamaFetchState` hook state to track fetching status (`loading`, `success`, `unreachable`, `no_models`).
2. Update the `fetchOllamaModels` logic inside `useEffect` in `SettingsPanel.tsx` to handle these states and set appropriate error messages.
3. Enhance the model selection UI when Ollama is selected:
   - Display a clean spinner when loading.
   - Display a descriptive error card and a text-fallback input + "Retry" button when unreachable.
   - Display an informative warning card with pull instructions, fallback input + "Retry" button when connected but no models exist.
   - Display a standard dropdown select with quick-select pills on success.
4. Verify the client builds cleanly and linters pass.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SettingsPanel State and Fetching Logic</name>
  <files>client/src/components/SettingsPanel.tsx</files>
  <read_first>client/src/components/SettingsPanel.tsx</read_first>
  <action>Add `ollamaFetchState` state variable. Update the `fetchOllamaModels` function to update state based on API response and handle caught network errors by transitioning to 'unreachable' state.</action>
  <verify>SettingsPanel.tsx compiles and updates state correctly during Ollama tags API calls.</verify>
  <acceptance_criteria>
    - `ollamaFetchState` is declared.
    - `fetchOllamaModels` sets state to loading, success, unreachable, or no_models correctly.
  </acceptance_criteria>
  <done>Updated fetching logic and state in SettingsPanel.</done>
</task>

<task type="auto">
  <name>Task 2: Build Dynamic Selector and Edge-case UI Layout</name>
  <files>client/src/components/SettingsPanel.tsx</files>
  <read_first>client/src/components/SettingsPanel.tsx</read_first>
  <action>Replace the Model Name input/select block in SettingsPanel.tsx to conditionally render the loading spinner, error cards, warning cards, and Retry buttons depending on the current `ollamaFetchState` value.</action>
  <verify>Frontend builds cleanly; dynamic components render as expected when switching providers in the UI settings panel.</verify>
  <acceptance_criteria>
    - Loading, unreachable, no_models, and success states are styled and rendered.
    - Retry buttons trigger `fetchOllamaModels`.
  </acceptance_criteria>
  <done>Built edge-case handling UI for Ollama model selection.</done>
</task>

<task type="auto">
  <name>Task 3: Run Builds and Linter Checks</name>
  <files></files>
  <read_first></read_first>
  <action>Run frontend compiler and linter script to verify the changes have no regression.</action>
  <verify>Frontend compilation and lint checks pass successfully.</verify>
  <acceptance_criteria>
    - `npm --prefix client run build` succeeds.
    - `npm --prefix client run lint` succeeds.
  </acceptance_criteria>
  <done>Verified client build and lint check.</done>
</task>

</tasks>

<verification>
- [ ] Frontend client builds cleanly (`npm run build` inside `client/`)
- [ ] Client linters pass with zero errors
</verification>

<success_criteria>
- Dynamic fetching automatically triggers when selecting Ollama.
- Spinner, error cards, warning states, and Retry buttons render cleanly for each state.
- Fallback manual input and Quick Select pills function as designed.
- Compiles with zero compilation or lint errors.
</success_criteria>

<output>
Create `.planning/quick/260622-ja9-implement-dynamic-model-fetching-for-loc/260622-ja9-SUMMARY.md` on completion.
</output>
