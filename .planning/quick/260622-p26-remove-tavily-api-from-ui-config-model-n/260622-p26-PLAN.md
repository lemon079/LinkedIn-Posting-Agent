---
phase: quick
plan: "260622-p26"
type: execute
wave: 1
depends_on: []
files_modified: ["client/src/components/SettingsPanel.tsx", "client/src/components/Header.tsx", "client/src/App.tsx"]
autonomous: true
requirements: []
must_haves:
  truths:
    - "Vite React client compiles and starts"
  artifacts:
    - path: "client/src/components/SettingsPanel.tsx"
      provides: "Updated configuration panel with Tavily API key removed, model selection conditional, and clean provider names"
      contains: "useMedia"
    - path: "client/src/components/Header.tsx"
      provides: "Responsive header with Node status and dot removed"
      contains: "Header"
    - path: "client/src/App.tsx"
      provides: "Updated settings panel integration and responsive instruction text"
      contains: "Configure parameters"
  key_links: []
---

<objective>
Improve Settings panel configuration and clean up Header responsiveness.
1. Fix the import of useMedia in `SettingsPanel.tsx` to be a named export: `import { useMedia } from "use-media";`.
2. Clean up LLM provider dropdown labels to exclude model names (Google, OpenAI, Anthropic, Ollama).
3. Hide the Model Name input field unless the provider is "ollama".
4. Remove the Tavily API Key configuration section from the Settings Panel UI and prop bindings.
5. Make Header responsive on mobile by shortening "Configure Credentials" to "Configure" on small viewports.
6. Remove "Node Online" text and the green status dot from the Header.
7. Make the "Configure parameters..." instruction text smaller, centered, and padded on small screens.
8. Verify client builds and linter checks pass.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit SettingsPanel.tsx to implement the requested UI cleanup and fix useMedia</name>
  <files>client/src/components/SettingsPanel.tsx</files>
  <read_first>client/src/components/SettingsPanel.tsx</read_first>
  <action>
    - Change useMedia import to `import { useMedia } from "use-media";`.
    - Change the options in LLM Provider `<select>` to only contain provider names (Google, OpenAI, Anthropic, Ollama).
    - Wrap the Model Name wrapper div in `provider === "ollama" && (...)` so it is only visible for Ollama.
    - Remove the Tavily Search API Key input block and clean up unused props / imports.
  </action>
  <verify>SettingsPanel.tsx compiles and the imports and UI render correctly.</verify>
  <acceptance_criteria>
    - useMedia import is corrected.
    - LLM Provider dropdown is simplified.
    - Model Name field is conditional on Ollama.
    - Tavily search API key field is removed from UI.
  </acceptance_criteria>
  <done>Modified SettingsPanel.tsx UI and imports.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Header and App layouts for responsiveness and indicator removals</name>
  <files>client/src/components/Header.tsx, client/src/App.tsx</files>
  <read_first>client/src/components/Header.tsx</read_first>
  <action>
    - Remove "Node Online" text and green status dot from Header.tsx.
    - Make padding and title text size responsive in Header.tsx.
    - Wrap the "Credentials" word in a `<span className="hidden sm:inline">` to save horizontal mobile space.
    - Update App.tsx settings props integration (remove Tavily bindings).
    - Make "Configure parameters..." text centered, smaller, and padded on mobile.
  </action>
  <verify>Header renders beautifully on narrow screens without wrapping/overlapping, status dot is gone, and home screen instruction wraps gracefully.</verify>
  <acceptance_criteria>
    - Status indicators removed.
    - Header and configure button are responsive.
    - Instruction text styled with responsive typography and padding.
  </acceptance_criteria>
  <done>Updated Header and App component layouts.</done>
</task>

<task type="auto">
  <name>Task 3: Run Builds and Linter Checks</name>
  <files></files>
  <read_first></read_first>
  <action>Run client build and linter to make sure there are no errors.</action>
  <verify>Frontend compilation and lint checks pass successfully.</verify>
  <acceptance_criteria>
    - `npm --prefix client run build` succeeds.
    - `npm --prefix client run lint` succeeds.
  </acceptance_criteria>
  <done>Verified client build and lint check.</done>
</task>

</tasks>

<verification>
- [ ] Frontend client builds cleanly (`npm run build` inside `client/`)
- [ ] Client linters pass with zero errors
</verification>

<success_criteria>
- The Settings Panel mounts without throwing the `useMedia` runtime error.
- LLM Provider dropdown has cleaned up labels (Google, OpenAI, Anthropic, Ollama).
- Model Name is visible only when Ollama is selected.
- Tavily key input is completely removed from the UI.
- Header fits on mobile screens without overflow, status dot/text is removed.
- Instruction text centers and shrinks gracefully on small screens.
- Frontend builds and lints cleanly.
</success_criteria>

<output>
Create `.planning/quick/260622-p26-remove-tavily-api-from-ui-config-model-n/260622-p26-SUMMARY.md` on completion.
</output>
