# Plan: Normalize model not found and related error messages

We want to normalize raw developer-facing error traces returned from LLM provider API failures into friendly, user-actionable messages.

## Proposed Changes

### Shared Utilities

#### [MODIFY] [utils.ts](file:///d:/Work/linkedin-agent/src/lib/utils.ts)
- Implement and export a shared `cleanErrorMessage` helper.
- The helper maps regex/substring lookups (such as API keys, model unsupported errors, network timeouts, rate limit quotas) to specific user-friendly message text.

### Hooks & Components

#### [MODIFY] [SettingsPanel.tsx](file:///d:/Work/linkedin-agent/src/components/SettingsPanel.tsx)
- Import `cleanErrorMessage` from `../lib/utils`.
- Remove the local copy of the clean error message function to avoid redundancy.

#### [MODIFY] [useAgent.ts](file:///d:/Work/linkedin-agent/src/hooks/useAgent.ts)
- Import `cleanErrorMessage` from `../lib/utils`.
- Wrap caught draft generation and post publishing errors inside `cleanErrorMessage` so the main screen shows clean feedback.
- Clean up cascading render ESLint warnings inside effects by replacing local state tracking (`prevSettingsOpen`) with a React `useRef` and wrapping synchronous settings updates in a deferred `setTimeout`.

## Verification Plan

### Automated Verification
- Run `npm run lint` to verify that there are no style, syntax, or cascading render issues.
- Run `npm run test` to verify that the unit tests pass cleanly.
