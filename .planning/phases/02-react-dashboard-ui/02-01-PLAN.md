---
phase: 02-react-dashboard-ui
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["client/src/App.tsx", "client/src/index.css", "client/vite.config.ts"]
autonomous: true
requirements: ["UI-01", "UI-04"]
must_haves:
  truths:
    - "Vite React client successfully boots on port 5173"
  artifacts:
    - path: "client/vite.config.ts"
      provides: "Vite server configurations with proxying to port 3000"
      contains: 'proxy:.*http://localhost:3000'
    - path: "client/src/App.tsx"
      provides: "Dashboard UI panel controls"
      contains: 'topic'
  key_links: []
---

<objective>
Initialize the React Vite frontend inside the client/ folder and build the dashboard control panel forms (topic selection, context input, dry-run toggle) with state bindings.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-react-dashboard-ui/02-CONTEXT.md
@.planning/phases/02-react-dashboard-ui/02-DISCUSSION-LOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Vite React TypeScript project</name>
  <files>client/package.json</files>
  <action>Create the client directory. Navigate into client/ and run the non-interactive Vite initialization command using react-ts template: npx -y create-vite@latest ./ --template react-ts --no-interactive. Once initialized, run npm install inside the client directory to fetch all template dependencies.</action>
  <verify>client/package.json contains react, react-dom and vite dependencies</verify>
  <acceptance_criteria>
    - The client directory contains standard Vite boilerplate files (vite.config.ts, src/main.tsx, package.json).
  </acceptance_criteria>
  <done>Vite project initialized and packages installed inside client/ directory</done>
</task>

<task type="auto">
  <name>Task 2: Configure Vite Server Proxy</name>
  <files>client/vite.config.ts</files>
  <read_first>client/vite.config.ts</read_first>
  <action>Open client/vite.config.ts. Configure it to support proxying `/api` requests to the Express server running on port 3000 (http://localhost:3000). Set up server: { proxy: { "/api": { target: "http://localhost:3000", changeOrigin: true } } }.</action>
  <verify>Vite proxy compiles cleanly</verify>
  <acceptance_criteria>
    - client/vite.config.ts contains proxy settings directing /api to port 3000
  </acceptance_criteria>
  <done>Vite proxy is configured to route API calls to Express</done>
</task>

<task type="auto">
  <name>Task 3: Configure Tailwind CSS v4 and Initialize shadcn UI</name>
  <files>client/src/index.css, client/vite.config.ts</files>
  <read_first>client/src/index.css, client/vite.config.ts</read_first>
  <action>Install Tailwind v4 and its Vite compiler: npm install tailwindcss @tailwindcss/vite inside client/. Import and configure tailwindcss() plugin in client/vite.config.ts. In client/src/index.css, clear all boilerplate styles and add @import "tailwindcss"; at the top. Once configured, initialize shadcn UI by running npx shadcn@latest init -y -t vite inside client/.</action>
  <verify>Tailwind and shadcn configure successfully without errors</verify>
  <acceptance_criteria>
    - client/vite.config.ts imports and loads the tailwindcss Vite plugin
    - client/src/index.css contains @import "tailwindcss"
    - client/components.json exists (created by shadcn init)
  </acceptance_criteria>
  <done>Tailwind CSS v4 configured and shadcn UI initialized inside client/</done>
</task>

<task type="auto">
  <name>Task 4: Install shadcn UI components and build Control Dashboard Layout</name>
  <files>client/src/App.tsx</files>
  <read_first>client/src/App.tsx</read_first>
  <action>Install required shadcn components: npx shadcn@latest add card button select textarea switch label -y inside client/. Open client/src/App.tsx. Replace boilerplate with a responsive split dashboard grid using Tailwind grid classes: Left panel for control forms, Right panel for output preview. On the Left, render a Shadcn Card containing: Topic Select dropdown (dynamic or placeholder), Custom Topic input, Context Textarea, and a Switch component for Dry Run. Bind inputs to React state hooks. Make sure design conforms to professional Slate Dark Mode aesthetics using Tailwind CSS classes. Keep under 80 lines.</action>
  <verify>client compiles and displays forms with tailwind styling</verify>
  <acceptance_criteria>
    - client/src/App.tsx contains shadcn Card, Select, Textarea, and Switch controls bound to React state
    - App renders cleanly using Tailwind styling
  </acceptance_criteria>
  <done>Shadcn components installed and state-bound control dashboard forms created in App.tsx</done>
</task>

</tasks>

<verification>
- [ ] Vite client builds and starts cleanly (npm run build inside client/)
- [ ] Form states modify correctly and reflect React bound inputs
</verification>

<success_criteria>
- Frontend SPA initialized inside client/ with fully configured Vite server proxy
- Modern responsive split layout controls built with Slate dark mode aesthetics
- Inputs for topic selection, custom topic, optional context, and dry-run state-managed
</success_criteria>

<output>
Create `.planning/phases/02-react-dashboard-ui/02-01-SUMMARY.md` on completion.
</output>
