---
phase: 06-file-image-uploading-feature-after-post-generation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["src/core/state.ts", "src/services/linkedin.ts", "src/graph/nodes/publishPost.ts", "src/app/api/publish/route.ts", "src/lib/api/agent.ts", "src/hooks/useAgent.ts", "src/components/EditorPanel.tsx", "src/components/LinkedInFeed.tsx", "src/app/api/media/upload/sign/route.ts", "src/tests/backend.test.ts"]
autonomous: true
requirements: ["MEDIA-01"]
must_haves:
  truths:
    - "Server generates pre-signed upload URLs for Supabase Storage temp-uploads bucket"
    - "Client uploads selected media files directly to Supabase Storage and passes storagePath to publish API"
    - "Server streams media from Supabase Storage readUrl directly to LinkedIn API"
    - "Server deletes temporary media files from Supabase Storage immediately after successful publish"
  artifacts:
    - path: "src/app/api/media/upload/sign/route.ts"
      provides: "Signed upload URL generation API route"
      contains: "createSignedUploadUrl"
    - path: "src/services/linkedin.ts"
      provides: "LinkedIn publishing logic with streaming download integration"
      contains: "fetch"
---

<objective>
Refactor and implement a secure, memory-efficient media uploading architecture using Supabase Storage. The browser uploads selected files (image/PDF) directly to a Supabase bucket (`temp-uploads`) using a pre-signed URL, and the server-side publishing agent streams the file from Supabase to LinkedIn, followed by a cleanup deletion of the temporary file from Supabase.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-file-image-uploading-feature-after-post-generation/06-CONTEXT.md
@.planning/phases/06-file-image-uploading-feature-after-post-generation/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AgentState annotation with updated mediaFile metadata shape</name>
  <files>src/core/state.ts</files>
  <read_first>src/core/state.ts</read_first>
  <action>Update the `mediaFile` type in `AgentState` in `src/core/state.ts` to support the new metadata: `{ name: string; type: string; storagePath?: string; readUrl?: string; base64?: string; } | null` with the reducer `(x, y) => y` and default value `null`.</action>
  <verify>npm run lint passes without error</verify>
  <acceptance_criteria>
    - `src/core/state.ts` contains the updated type definition for `mediaFile` supporting `storagePath` and `readUrl`.
  </acceptance_criteria>
  <done>AgentState mediaFile type signature updated</done>
</task>

<task type="auto">
  <name>Task 2: Implement Signed URL API Route</name>
  <files>src/app/api/media/upload/sign/route.ts</files>
  <read_first>src/services/supabase.ts</read_first>
  <action>Create a new Next.js route handler `src/app/api/media/upload/sign/route.ts` that exports a GET request handler. It must:
  1. Retrieve request authorization and check for authenticated user using `getRequestAuth`.
  2. Parse `filename` and `mimeType` from the request query parameters.
  3. Verify if the server-side Supabase client (`supabase` service role client) is configured. If not, return `{ localMode: true }` to signify fallback to local memory upload.
  4. Generate a unique path in the bucket: `temp/${userId || 'anonymous'}/${Date.now()}-${filename}`.
  5. Call `supabase.storage.from("temp-uploads").createSignedUploadUrl(path)` to get a signed upload URL.
  6. Generate the public `readUrl` using `supabase.storage.from("temp-uploads").getPublicUrl(path).data.publicUrl`.
  7. Return the signed `uploadUrl`, the `storagePath` (path), and the public `readUrl` in a JSON response.</action>
  <verify>npx tsc --noEmit compiles successfully</verify>
  <acceptance_criteria>
    - File `src/app/api/media/upload/sign/route.ts` exists.
    - Exports a `GET` function handling signed URL generation using `createSignedUploadUrl`.
    - Handles local BYOK fallback when Supabase keys are missing.
  </acceptance_criteria>
  <done>Signed URL API Route implemented</done>
</task>

<task type="auto">
  <name>Task 3: Refactor useAgent and EditorPanel to upload directly to Supabase</name>
  <files>src/hooks/useAgent.ts, src/components/EditorPanel.tsx, src/lib/api/agent.ts</files>
  <read_first>src/hooks/useAgent.ts, src/components/EditorPanel.tsx, src/lib/api/agent.ts</read_first>
  <action>1. In `src/lib/api/agent.ts`, update `publishPost` file type signature to match `{ name: string; type: string; storagePath?: string; readUrl?: string; base64?: string; }`.
  2. In `src/hooks/useAgent.ts`, update file upload logic. When a file is selected:
     - Check if public `supabase` client is available.
     - If `supabase` client is null (local mode fallback): Read file as base64 and store `{ name: file.name, type: file.type, base64: reader.result }` in `selectedFile`.
     - If `supabase` client is active:
       - Make a GET request to `/api/media/upload/sign` with query params `filename` and `mimeType`.
       - Perform a PUT request to the returned `uploadUrl` with the file blob/bytes in the body.
       - Store `{ name: file.name, type: file.type, storagePath, readUrl }` in `selectedFile`.
  3. In `src/components/EditorPanel.tsx`, add an upload loading state indicator while the file is transferring to Supabase Storage, and prevent publishing until upload completes.</action>
  <verify>npm run lint passes without error</verify>
  <acceptance_criteria>
    - Client hook handles direct-to-Supabase upload via signed URL.
    - Client hook falls back to base64 transmission in local BYOK mode.
    - EditorPanel shows a loading state during upload.
  </acceptance_criteria>
  <done>Client-side upload flow and UI indicators integrated</done>
</task>

<task type="auto">
  <name>Task 4: Update LinkedIn publish service and publishPost graph node to stream media and clean up</name>
  <files>src/services/linkedin.ts, src/graph/nodes/publishPost.ts, src/app/api/publish/route.ts</files>
  <read_first>src/services/linkedin.ts, src/graph/nodes/publishPost.ts, src/app/api/publish/route.ts</read_first>
  <action>1. In `src/services/linkedin.ts`, update `publishLinkedInPost` to accept the new `mediaFile` structure. If `mediaFile.readUrl` is provided:
     - Perform a `fetch(mediaFile.readUrl)` to get the stream body.
     - Register the asset on LinkedIn.
     - Stream the downloaded body stream directly into LinkedIn's PUT request: `body: downloadRes.body` (requires removing headers/tokens that fail signature checks).
     - If local mode base64 fallback is used (`mediaFile.base64` is provided), continue to use the legacy base64 decoding buffer upload logic.
  2. In `src/graph/nodes/publishPost.ts`, after successful UGC post creation:
     - If `state.mediaFile.storagePath` exists, invoke the Supabase admin client to delete the temporary file from the bucket: `supabase.storage.from("temp-uploads").remove([storagePath])`.
  3. In `src/app/api/publish/route.ts`, accept the new file payload structure in the POST request body and pass it through to the LangGraph state update.</action>
  <verify>npm run lint passes without error</verify>
  <acceptance_criteria>
    - `publishLinkedInPost` downloads the media from `readUrl` and streams it directly to LinkedIn's PUT URL.
    - `publishPost` node deletes the temp file from Supabase Storage after successful publish.
    - `/api/publish` accepts the new file format.
  </acceptance_criteria>
  <done>Backend publishing stream-to-stream integration and cleanup complete</done>
</task>

<task type="auto">
  <name>Task 5: Update LinkedInFeed component and backend tests</name>
  <files>src/components/LinkedInFeed.tsx, src/tests/backend.test.ts</files>
  <read_first>src/components/LinkedInFeed.tsx, src/tests/backend.test.ts</read_first>
  <action>1. In `src/components/LinkedInFeed.tsx`, update the render layout to display the image using the file's `readUrl` or local object URL preview, or base64 fallback.
  2. In `src/tests/backend.test.ts`, update or add mock coverage to assert the new streaming and cleanup path (verify the removal and fetching). Ensure mocks for Supabase Storage do not fail.</action>
  <verify>npm test passes successfully</verify>
  <acceptance_criteria>
    - Feed simulator renders image previews.
    - Backend unit tests pass without error.
  </acceptance_criteria>
  <done>LinkedInFeed and unit tests updated for new upload flow</done>
</task>

</tasks>

<verification>
- [ ] npm run lint passes without errors
- [ ] npx tsc --noEmit compiles successfully
- [ ] npm test runs and all unit tests pass successfully
- [ ] Verify signed URL request `/api/media/upload/sign` returns correctly
- [ ] Verify image selection performs direct PUT request to the signed URL
- [ ] Verify post publication successfully downloads and streams file to LinkedIn, followed by deletion from Supabase Storage
- [ ] Verify local mode fallback works (e.g. when Supabase keys are not set, base64 is still uploaded on publish)
</verification>

<success_criteria>
- Backend API handles streaming uploads from Supabase to LinkedIn and deletes temp files
- Client-side uploads media directly to Supabase Storage via signed URL and renders preview
- Safe, non-breaking local BYOK base64 fallback behaves correctly when Supabase keys are absent
</success_criteria>

<output>
Create `.planning/phases/06-file-image-uploading-feature-after-post-generation/06-01-SUMMARY.md` on completion.
</output>
