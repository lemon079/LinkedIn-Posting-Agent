---
phase: 06-file-image-uploading-feature-after-post-generation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["src/core/state.ts", "src/services/linkedin.ts", "src/graph/nodes/publishPost.ts", "src/app/api/publish/route.ts", "src/lib/api/agent.ts", "src/hooks/useAgent.ts", "src/components/EditorPanel.tsx", "src/components/LinkedInFeed.tsx", "src/app/page.tsx"]
autonomous: true
requirements: ["MEDIA-01"]
must_haves:
  truths:
    - "Backend API supports base64 file publishing"
    - "UI supports file uploading and previewing in the feed"
  artifacts:
    - path: "src/services/linkedin.ts"
      provides: "LinkedIn image and PDF upload and post publication"
      contains: "urn:li:digitalmediaRecipe"
---

<objective>
Implement file and image uploading for posts, allowing users to upload a single JPEG/PNG/WebP image or PDF document up to 4MB in the editor, preview it in the LinkedIn Feed tab, and publish it natively to LinkedIn via the server-side API.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-file-image-uploading-feature-after-post-generation/06-CONTEXT.md
@.planning/phases/06-file-image-uploading-feature-after-post-generation/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mediaFile to AgentState annotation</name>
  <files>src/core/state.ts</files>
  <read_first>src/core/state.ts</read_first>
  <action>Add a `mediaFile` property to the `AgentState` annotation object in `src/core/state.ts`. The property should have type: `{ name: string; type: string; base64: string; } | null` with a reducer `(x, y) => y` and default value returning `null`.</action>
  <verify>npm run lint passes without error</verify>
  <acceptance_criteria>
    - src/core/state.ts contains definition for mediaFile property inside AgentState
    - type of mediaFile is `{ name: string; type: string; base64: string; } | null` or equivalent type definition
  </acceptance_criteria>
  <done>AgentState updated with mediaFile field</done>
</task>

<task type="auto">
  <name>Task 2: Support registering and uploading media in publishLinkedInPost service</name>
  <files>src/services/linkedin.ts</files>
  <read_first>src/services/linkedin.ts</read_first>
  <action>Update `publishLinkedInPost` function in `src/services/linkedin.ts` to accept an optional fourth argument `mediaFile?: { name: string; type: string; base64: string; }`. If `mediaFile` is provided:
  1. Determine the correct recipe based on `mediaFile.type` (images use `urn:li:digitalmediaRecipe:feedshare-image`; PDF uses `urn:li:digitalmediaRecipe:document-preview`).
  2. Parse the base64 string `mediaFile.base64` (after removing the data URI scheme prefix like `data:*/*;base64,`) and convert it to a Buffer: `Buffer.from(base64Data, "base64")`.
  3. Register the upload by POSTing to `https://api.linkedin.com/v2/assets?action=registerUpload`. Use the correct recipe and the current user's author URN (`customUrn` or `config.LINKEDIN_PERSON_URN`).
  4. Extract `uploadUrl` and `asset` URN from the registration response.
  5. Upload the Buffer to the pre-signed `uploadUrl` using a `PUT` request with headers `Content-Type: application/octet-stream` (ensure Authorization header is NOT included).
  6. In the UGC post payload sent to `/v2/ugcPosts`, set `shareMediaCategory` to `"IMAGE"` (for image types) or `"NATIVE_DOCUMENT"` (for PDF). Populate `media` array with a single object containing `status: "READY"`, `media: asset`, `title: { text: mediaFile.name }`, and description.</action>
  <verify>npm run lint passes without error</verify>
  <acceptance_criteria>
    - publishLinkedInPost signature includes mediaFile parameter
    - calls registerUpload endpoint when mediaFile is provided
    - PUT request is made to upload URL with binary buffer
    - shareMediaCategory is set to NATIVE_DOCUMENT for PDFs and IMAGE for images
  </acceptance_criteria>
  <done>LinkedIn publish service supports file registration, binary upload, and publishing with media asset URN</done>
</task>

<task type="auto">
  <name>Task 3: Update publishPost node and publish API route to pass mediaFile</name>
  <files>src/graph/nodes/publishPost.ts, src/app/api/publish/route.ts</files>
  <read_first>src/graph/nodes/publishPost.ts, src/app/api/publish/route.ts</read_first>
  <action>1. In `src/graph/nodes/publishPost.ts`, update the `publishLinkedInPost` call to pass `state.mediaFile || undefined` as the fourth parameter.
  2. In `src/app/api/publish/route.ts`, parse `file` from the request JSON body. In the `agent.updateState` call, include `mediaFile: file || null` in the state update parameters.</action>
  <verify>npm run lint passes without error</verify>
  <acceptance_criteria>
    - src/graph/nodes/publishPost.ts passes state.mediaFile to publishLinkedInPost
    - src/app/api/publish/route.ts parses file from body and includes it in agent.updateState
  </acceptance_criteria>
  <done>Publish node and route updated to bridge media file state between client and server</done>
</task>

<task type="auto">
  <name>Task 4: Update useAgent and client API library to track and send files</name>
  <files>src/lib/api/agent.ts, src/hooks/useAgent.ts</files>
  <read_first>src/lib/api/agent.ts, src/hooks/useAgent.ts</read_first>
  <action>1. In `src/lib/api/agent.ts`, update `publishPost` function signature and body to accept an optional `file?: { name: string; type: string; base64: string; }` and include it in the POST body to `/api/publish`.
  2. In `src/hooks/useAgent.ts`, add a state variable: `const [selectedFile, setSelectedFile] = useState<{ name: string; type: string; base64: string; } | null>(null);`. Return `selectedFile` and `setSelectedFile` from the hook. Update `handlePublish` and `handleGenerate` to:
     - Clear `selectedFile` on generate.
     - Pass `selectedFile || undefined` to `publishPost`.
     - Reset `selectedFile` to `null` after successful publication.</action>
  <verify>npm run lint passes without error</verify>
  <acceptance_criteria>
    - src/lib/api/agent.ts sends file property in publishPost JSON body
    - src/hooks/useAgent.ts manages selectedFile state and clears/passes it appropriately
  </acceptance_criteria>
  <done>Client-side API and useAgent hook support managing and sending the selected media file</done>
</task>

<task type="auto">
  <name>Task 5: Integrate file selector and preview UI inside EditorPanel and LinkedInFeed</name>
  <files>src/components/EditorPanel.tsx, src/components/LinkedInFeed.tsx, src/app/page.tsx</files>
  <read_first>src/components/EditorPanel.tsx, src/components/LinkedInFeed.tsx, src/app/page.tsx</read_first>
  <action>1. In `src/components/EditorPanel.tsx`, add file input field and styling. Allow selecting files. Restrict file selector using `accept="image/png, image/jpeg, image/webp, application/pdf"`. Read files using `FileReader.readAsDataURL` to get base64 string. Validate file size <= 4MB and show alert/error if size is exceeded. When a file is selected, show file name, thumbnail/icon (e.g. PDF file icon, or image thumbnail), and a "Remove File" button to reset the file.
  2. In `src/components/LinkedInFeed.tsx`, update the render output. If `mediaFile` or `selectedFile` is present, display it under the commentary. For images, render an `<img>` tag showing the base64 URL. For PDFs, render a document box with file name, PDF icon, and pages metadata placeholder (representing LinkedIn document attachment look).
  3. In `src/app/page.tsx`, destructure `selectedFile` and `setSelectedFile` from `agentState`. Pass them into `EditorPanel` and `LinkedInFeed` so the file state is shared and updated dynamically.</action>
  <verify>npm run lint passes without error</verify>
  <acceptance_criteria>
    - EditorPanel allows selecting files and validates size <= 4MB
    - EditorPanel shows thumbnail/filename and a remove button when file is selected
    - LinkedInFeed renders image or PDF preview under post text
    - page.tsx successfully passes file state down to components
  </acceptance_criteria>
  <done>File upload UI and simulated LinkedIn feed preview are fully implemented and integrated</done>
</task>

</tasks>

<verification>
- [ ] npm run lint passes without errors
- [ ] npx tsc --noEmit compiles successfully
- [ ] Local tests run and pass (run npm test if test runner exists, or run build/dev verification)
- [ ] Select an image in the UI, verify it is previewed inside the Editor and LinkedIn Feed tabs correctly
- [ ] Select a PDF in the UI, verify it displays document preview layout in the Feed tab
- [ ] Verify file sizes > 4MB trigger client-side validation errors
</verification>

<success_criteria>
- Backend API `/api/publish` handles base64 image/PDF uploads to LinkedIn
- Feed simulator renders high-fidelity image previews and document attachment preview boxes
- Complete client-side upload size validation and file clearance flow in the editor
</success_criteria>

<output>
After completion, create `.planning/phases/06-file-image-uploading-feature-after-post-generation/06-01-SUMMARY.md`
</output>
