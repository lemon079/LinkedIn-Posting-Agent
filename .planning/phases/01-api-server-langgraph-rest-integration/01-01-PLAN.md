---
phase: 01-api-server-langgraph-rest-integration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["package.json", "src/server.ts"]
autonomous: true
requirements: ["API-01"]
must_haves:
  truths:
    - "GET /api/topics returns the array of default genres"
  artifacts:
    - path: "src/server.ts"
      provides: "Express HTTP server startup and routing"
      contains: 'app\.get\("/api/topics"'
  key_links: []
---

<objective>
Bootstrap the Express API server boilerplate and implement the topics endpoint to retrieve default genres.

Purpose: Set up the HTTP foundation that the frontend Vite client will communicate with, exposing default post categories.
Output: Integrated dependencies inside package.json, and a booting Express server in src/server.ts.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/config/env.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Express and CORS dependencies</name>
  <files>package.json</files>
  <read_first>package.json</read_first>
  <action>Install Express and CORS packages by adding dependencies in package.json: "express" (v4.21.2 or latest) and "cors" (v2.8.5). Add "@types/express" and "@types/cors" to devDependencies. Add a start command inside "scripts": "server": "tsx src/server.ts".</action>
  <verify>npm run lint passes without error</verify>
  <acceptance_criteria>
    - package.json contains "express" and "cors" in dependencies
    - package.json contains "@types/express" and "@types/cors" in devDependencies
    - package.json contains "server": "tsx src/server.ts" inside scripts
  </acceptance_criteria>
  <done>Express and CORS are listed in package.json dependencies and devDependencies</done>
</task>

<task type="auto">
  <name>Task 2: Implement Express server and topics route</name>
  <files>src/server.ts</files>
  <read_first>src/config/env.ts, src/scheduler.ts</read_first>
  <action>Create src/server.ts. Configure it as an ES module. Import express, cors, and config. Set up an Express application. Configure CORS middleware with origin whitelist: "http://localhost:5173". Configure express.json() body parsing middleware. Implement a "GET /api/topics" endpoint that responds with status 200 and the JSON array: ["AI", "Machine Learning", "Generative AI", "Frontend", "Backend", "System Design", "Cloud"]. Bind the server to port 3000, and print a startup success message containing port info.</action>
  <verify>npx tsx src/server.ts starts and curl http://localhost:3000/api/topics returns the array</verify>
  <acceptance_criteria>
    - src/server.ts contains app.get("/api/topics" endpoint returning the topics list
    - src/server.ts contains cors({ origin: "http://localhost:5173" }) or similar configuration
    - src/server.ts contains app.listen(3000) or similar configuration
  </acceptance_criteria>
  <done>Express server boots on port 3000 and successfully serves GET /api/topics</done>
</task>

</tasks>

<verification>
- [ ] dependencies are fully installed (run npm install)
- [ ] tsc compiles without errors (run npm run build or npx tsc --noEmit)
- [ ] GET /api/topics returns the array of default genres
</verification>

<success_criteria>
- Express boilerplate successfully compiled and bootable
- CORS whitelisting enabled for local Vite UI
- /api/topics endpoint serving JSON genres array
</success_criteria>

<output>
After completion, create `.planning/phases/01-api-server-langgraph-rest-integration/01-01-SUMMARY.md`
</output>
