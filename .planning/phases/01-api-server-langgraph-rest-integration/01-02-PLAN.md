---
phase: 01-api-server-langgraph-rest-integration
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: ["src/server.ts"]
autonomous: true
requirements: ["API-02", "API-03"]
must_haves:
  truths:
    - "POST /api/draft generates a draft post and interrupts execution"
    - "POST /api/publish updates thread state and publishes to LinkedIn"
  artifacts:
    - path: "src/server.ts"
      provides: "Express HTTP server routing with LangGraph controllers"
      contains: 'app\.post\("/api/draft"'
    - path: "src/server.ts"
      provides: "Express HTTP server routing with LangGraph controllers"
      contains: 'app\.post\("/api/publish"'
  key_links:
    - from: "src/server.ts"
      to: "src/graph/index.ts"
      via: "importing compiled agent"
      pattern: 'import.*agent.*from.*graph/index\.js'
---

<objective>
Implement the POST /api/draft and POST /api/publish REST endpoints integrating Express with LangGraph's state graph.

Purpose: Expose endpoints to start agent execution (generating drafts with Gemini/Tavily), hold threads at breakpoints, allow draft edits, and publish posts to LinkedIn.
Output: Integrated endpoints inside src/server.ts that import the agent and manage MemorySaver checkpointer threads.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-api-server-langgraph-rest-integration/01-01-PLAN.md
@src/server.ts
@src/graph/index.ts
@src/scheduler.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the draft post API route</name>
  <files>src/server.ts</files>
  <read_first>src/server.ts, src/graph/index.ts, src/scheduler.ts</read_first>
  <action>Open src/server.ts. Import "agent" from "./graph/index.js". Implement app.post("/api/draft", ...) endpoint. Extract optional "topic" and "context" from request body. If topic is missing, select a random genre using the same logic and list as in src/scheduler.ts. Generate a unique thread ID using Date.now().toString() and construct a threadConfig: { configurable: { thread_id: threadId } }. Invoke the agent with initialState containing the topic, context, postContent: null, postUrl: null, retries: 0, error: null. Run agent.invoke(initialState, threadConfig). Fetch the paused state: agent.getState(threadConfig). Assert that the next executing node is "publishPost". Return JSON: { threadId, draft: state.values.postContent, status: "needs_approval" }. Wrap the route logic in a try/catch, logging the error and returning status 500 with { error: msg } if it fails.</action>
  <verify>npx tsx src/server.ts starts and a POST request to http://localhost:3000/api/draft returning the draft and threadId</verify>
  <acceptance_criteria>
    - src/server.ts contains app.post("/api/draft" route
    - src/server.ts imports the agent from "./graph/index.js"
    - The API response contains "threadId" and "draft" properties on successful drafting
  </acceptance_criteria>
  <done>POST /api/draft triggers the agent, interrupts at the publishPost breakpoint, and returns draft copy with threadId</done>
</task>

<task type="auto">
  <name>Task 2: Implement the publish post API route</name>
  <files>src/server.ts</files>
  <read_first>src/server.ts, src/graph/index.ts, src/services/linkedin.ts</read_first>
  <action>Open src/server.ts. Implement app.post("/api/publish", ...) endpoint. Extract required "threadId" and "draft" (representing final client-edited text) from the request body. If either is missing, return status 400 with { error: "Missing threadId or draft" }. Set up threadConfig: { configurable: { thread_id: threadId } }. Fetch the thread state: await agent.getState(threadConfig). If no state is found or next node is not "publishPost", return status 400 with { error: "Invalid thread or post already published" }. Call agent.updateState(threadConfig, { postContent: draft }) to overwrite the thread state with the client's edits. Resume the graph run: const finalState = await agent.invoke(null, threadConfig). If finalState.error is present, return status 500 with { error: finalState.error }. Otherwise, return status 200 with { postUrl: finalState.postUrl }.</action>
  <verify>POST request to http://localhost:3000/api/publish with valid threadId and draft returns the postUrl</verify>
  <acceptance_criteria>
    - src/server.ts contains app.post("/api/publish" route
    - src/server.ts contains agent.updateState(threadConfig, { postContent: ... }) to save client edits
    - The API response contains "postUrl" on successful publication
  </acceptance_criteria>
  <done>POST /api/publish resumes the agent thread with client edits and publishes to LinkedIn</done>
</task>

</tasks>

<verification>
- [ ] tsc compiles without errors (run npx tsc --noEmit)
- [ ] POST /api/draft creates and pauses a thread
- [ ] POST /api/publish updates and successfully executes the thread
</verification>

<success_criteria>
- Endpoints POST /api/draft and POST /api/publish implemented and verified
- Edited text changes saved using agent.updateState before publishing
- Thread error handling captures exceptions and returns 500 status
</success_criteria>

<output>
After completion, create `.planning/phases/01-api-server-langgraph-rest-integration/01-02-SUMMARY.md`
</output>
