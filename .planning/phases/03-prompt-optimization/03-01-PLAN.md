---
phase: 03-prompt-optimization
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["src/core/prompts.ts", "src/core/examples.ts"]
autonomous: true
requirements: ["PRMPT-01"]
must_haves:
  truths:
    - "System prompt module compiles and contains few-shot examples"
  artifacts:
    - path: "src/core/examples.ts"
      provides: "Few-shot examples of technical LinkedIn posts under 80 lines"
      contains: "Kafka"
    - path: "src/core/prompts.ts"
      provides: "System prompt constructed with few-shot examples"
      contains: "FEW_SHOT_EXAMPLES"
  key_links: []
---

<objective>
Refine the system prompt for the ghostwriter agent by extracting the system prompt to include few-shot examples, separating the prompt configurations to respect the 80-line file limit, and optimizing output formatting for LinkedIn.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-prompt-optimization/03-CONTEXT.md
@.planning/phases/03-prompt-optimization/03-DISCUSSION-LOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Few-Shot Examples Module</name>
  <files>src/core/examples.ts</files>
  <action>Create a new file `src/core/examples.ts`. Define and export three distinct string constants representing the high-fidelity technical writing examples (Kafka rebalances, Postgres MVCC bloat, React Suspense waterfalls). Each example must be a real technical post following the whiteboard tone, correct spacing, exactly 2 emojis, no markdown asterisks, and end with a question. Make sure the total file size remains strictly under 80 lines.</action>
  <verify>src/core/examples.ts exists and compiles cleanly without errors</verify>
  <acceptance_criteria>
    - src/core/examples.ts contains three exported few-shot post content strings
    - Total line count of src/core/examples.ts is under 80 lines
  </acceptance_criteria>
  <done>Created src/core/examples.ts containing the few-shot templates</done>
</task>

<task type="auto">
  <name>Task 2: Update Prompts Module and Integrate Few-Shots</name>
  <files>src/core/prompts.ts</files>
  <read_first>src/core/prompts.ts, src/core/examples.ts</read_first>
  <action>Open src/core/prompts.ts. Import the three few-shot examples from `./examples.js`. Modify the `SYSTEM_PROMPT` constant to append a section titled "FEW-SHOT EXAMPLES" where the three examples are displayed with clear demarcations (e.g. "Example 1:", "Example 2:", etc.). Update instructions to prompt the LLM to follow these style examples strictly. Ensure the entire file remains under 80 lines.</action>
  <verify>src/core/prompts.ts compiles and the SYSTEM_PROMPT contains the imported few-shots</verify>
  <acceptance_criteria>
    - src/core/prompts.ts imports examples from `./examples.js`
    - SYSTEM_PROMPT contains the imported examples
    - Total line count of src/core/prompts.ts is under 80 lines
  </acceptance_criteria>
  <done>SYSTEM_PROMPT updated with few-shot context integration</done>
</task>

</tasks>

<verification>
- [ ] Backend runs compile task cleanly (`npm run build` or `npx tsc --noEmit`)
- [ ] Agent triggers and generates drafts that correctly match the few-shot styling (no markdown asterisks, 2 emojis, whiteboard-style tone)
</verification>

<success_criteria>
- Three high-fidelity few-shot writing examples integrated into prompts configuration
- System prompts codebase decomposed so all files remain strictly under 80 lines
- Draft quality aligned to true LinkedIn feed formatting specifications
</success_criteria>

<output>
Create `.planning/phases/03-prompt-optimization/03-01-SUMMARY.md` on completion.
</output>
