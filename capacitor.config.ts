import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lemon079.linkedinagent',
  appName: 'LinkedIn Posting Agent',
  webDir: 'out'
};

export default config;
